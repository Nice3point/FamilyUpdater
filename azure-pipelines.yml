# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [ ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: InitializeBuilder
        displayName: 'InitializeBuilder'
        dependsOn: [ ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './.build/build.cmd InitializeBuilder --skip'
      - job: Cleaning
        displayName: 'Cleaning'
        dependsOn: [ ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './.build/build.cmd Cleaning --skip'
      - job: Compile
        displayName: 'Compile'
        dependsOn: [ ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './.build/build.cmd Compile --skip'
      - job: CreateBundle
        displayName: 'CreateBundle'
        dependsOn: [ ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './.build/build.cmd CreateBundle --skip'
      - job: ZipBundle
        displayName: 'ZipBundle'
        dependsOn: [ ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './.build/build.cmd ZipBundle --skip'
      - job: CreateInstaller
        displayName: 'CreateInstaller'
        dependsOn: [ ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './.build/build.cmd CreateInstaller --skip'
